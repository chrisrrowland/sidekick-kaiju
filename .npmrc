# Refuse to install a package version published within the last 7 days (10080
# minutes) — mitigates supply-chain attacks where a malicious version is published
# and then quickly unpublished/flagged before most consumers install it.
minimum-release-age=10080
